{
  "name": "pdpl-reviewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "build:pages": "tsc -b && VITE_BASE_URL= vite build",
    "deploy:pages": "npm run build:pages && pac pages upload-code-site --siteName \"PDPL Reviewer\" --rootDirectory dist",
    "deploy:functions": "cd azure-functions && npm install && npm run build && func azure functionapp publish pdpl-reviewer-api",
    "setup:dataverse": "node scripts/setup-dataverse.mjs",
    "create:user": "node scripts/create-user.mjs"
  },
  "dependencies": {
    "@azure/msal-browser": "^4.30.0",
    "@azure/msal-common": "^15.17.0",
    "fflate": "^0.8.3",
    "lucide-react": "^1.16.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0"
  },
  "devDependencies": {
    "@azure/identity": "^4.13.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "~5.7.0",
    "vite": "^6.0.0"
  }
}
